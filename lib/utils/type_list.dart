enum MessageType { text, image, video, doc, location, contact, audio, messageType,gif, link, imageArray,note, chatLoading,emoji }
enum StatusType { text, image, video}

enum PositionItemType {
  log,
  position,
}